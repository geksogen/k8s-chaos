apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: partition
  namespace: chaos-testing
spec:
  action: partition
  mode: all
  selector:
    labelSelectors:
      "app": "mycurlpod"
  direction: to
  target:
    selector:
      labelSelectors:
        "app": "chaos-app-backend"
    mode: all
  duration: "50s"