apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-httpd
spec:
  action: pod-kill
  mode: one
  duration: '30s'
  selector:
    labelSelectors:
      "app.kubernetes.io/component": "httpd"
  scheduler:
    cron: "@every 2m"