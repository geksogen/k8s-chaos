apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-httpd
  namespace: chaos-testing
spec:
  action: pod-kill
  mode: one
  duration: '60s'
  selector:
    namespaces:
      - chaos-app
    labelSelectors:
      "app": "httpd"
  scheduler:
    cron: "@every 2m"